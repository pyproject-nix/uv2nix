[project]
name = "local-index-sdist"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["attrs"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
find-links = ["../kitchen-sink/"]
no-binary-package = ["attrs"]
