[project]
name = "pkg-b"
version = "0.1.0"
description = "Package B"
requires-python = ">=3.12"
dependencies = [
    "pkg-a",
    "click>=8.1.0",
]

[tool.uv.sources]
pkg-a = { workspace = true }

[tool.uv]
package = true