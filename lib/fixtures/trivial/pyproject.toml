[project]
name = "trivial"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "arpeggio>=2.0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
