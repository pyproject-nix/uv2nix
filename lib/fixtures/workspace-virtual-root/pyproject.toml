[project]
name = "workspace-virtual-root"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pkg-a",
    "arpeggio>=2.0.2",
]

[tool.uv.sources]
pkg-a = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]

[tool.uv]
package = false
