[project]
name = "shared"
version = "0.1.0"
description = "Shared library component"
requires-python = ">=3.12"
dependencies = [
    "pydantic",
    "typing-extensions",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
